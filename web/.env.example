# Public site URL used for absolute Open Graph tags during build.
PUBLIC_SITE_URL=http://localhost
# Interactive download UI (PR8). Only "true" or "false". Default false.
PUBLIC_MEDIA_FLOW_ENABLED=false
